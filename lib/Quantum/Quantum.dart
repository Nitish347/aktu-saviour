import 'package:flutter/material.dart';
class QuantumScreen extends StatelessWidget {
  // const QuantumScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("quantum"),
    );
  }
}
